<h1><%= @event.even_name %></h1>
<p>
  <%= @event.description %>
</p>
<%= @event.date %>
<h2>Attendees</h2>
<ul>
  <%= @attendees.each do |at| %>
  <% id = at.attendee_id %>
  <li><%=User.where(id: id).first.email %></li>
  

 
  <% end %>
</ul>

 

