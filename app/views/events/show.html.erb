<h1><%= @event.even_name %></h1>
<p>
  <%= @event.description %>
</p>
<%= @event.date %>
<h2>Attendees</h2>
<% @attendees.each do |at| %>
  <% id = at.attendee_id %>
  <%= User.find(params[:id]).email%>
<% end %>
